{% extends "base" %}
{% block content %}
<h1>Showing showroom piece {{name}}</h1>
    <h3>Here are the items:</h3>
    <ul>
        {% for s in items %}
            <li>{{ s }}</li>
        {% endfor %}
    </ul>


{% endblock content %}
